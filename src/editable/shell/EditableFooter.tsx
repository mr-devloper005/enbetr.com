'use client'
import Link from 'next/link'
import { SITE_CONFIG } from '@/lib/site-config'
import { useEditableLocalAuthSession } from '@/editable/components/EditableLocalAuthForms'
export function EditableFooter(){const {session,logout}=useEditableLocalAuthSession();return <footer className="bg-[#11090c] text-white"><div className="mx-auto grid max-w-[var(--editable-container)] gap-10 px-5 py-14 sm:px-8 md:grid-cols-2 lg:grid-cols-4"><div><div className="flex items-center gap-3"><span className="flex h-10 w-10 items-center justify-center overflow-hidden rounded-xl bg-[#951137]"><img src="/favicon.png" alt={`${SITE_CONFIG.name} logo`} className="h-full w-full object-contain" /></span><b className="text-2xl">{SITE_CONFIG.name}</b></div><p className="mt-4 max-w-sm text-sm leading-7 text-white/55">A focused place to discover classifieds, showcase professional profiles, and make useful connections.</p></div><div><h3 className="text-xs font-bold uppercase tracking-[.2em] text-[#d6ac50]">Explore</h3><div className="mt-4 grid gap-3 text-sm text-white/65"><Link href="/classified">Classified</Link><Link href="/">Home</Link><Link href="/about">About</Link><Link href="/search">Search</Link></div></div><div><h3 className="text-xs font-bold uppercase tracking-[.2em] text-[#d6ac50]">Connect</h3><div className="mt-4 grid gap-3 text-sm text-white/65"><Link href="/contact">Contact</Link>{session?<Link href="/create">Create</Link>:null}</div></div><div><h3 className="text-xs font-bold uppercase tracking-[.2em] text-[#d6ac50]">Account</h3><div className="mt-4 flex flex-wrap gap-2">{session?<button onClick={logout} className="rounded-lg border border-white/20 px-4 py-2 text-sm font-bold">Logout</button>:<><Link href="/login" className="rounded-lg border border-white/20 px-4 py-2 text-sm font-bold">Login</Link><Link href="/signup" className="rounded-lg bg-[#941137] px-4 py-2 text-sm font-bold">Sign up</Link></>}</div></div></div><div className="border-t border-white/10 px-5 py-5 text-center text-xs text-white/40">© {new Date().getFullYear()} {SITE_CONFIG.name}. Built for useful local connections.</div></footer>}
